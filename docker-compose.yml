version: '3.4'

services:
  testdeployblazor:
    image: testdeployblazor
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3005:80"
      - "3006:443"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - DOTNET_WATCH=1
